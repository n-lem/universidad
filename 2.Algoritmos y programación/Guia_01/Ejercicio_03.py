# 3. Escribir un programa que almacene en distintas variables, dos valores 
# enteros, dos reales, dos string y muestre el tipo de dato con la instrucción 
# type() de todas las variables.

# Almaceno en dos variables dos valores enteros
variable1 = 4
variable2 = 6

# Almaceno en dos variables dos valores reales
variable3 = 9.5
variable4 = 4325.4

# Almaceno en dos variables dos valores string
variable5 = 'Hola'
variable6 = '4324'

# Muestro el tipo de dato de cada variable ingresada
print(variable1, 'es del tipo', type(variable1))
print(variable2, 'es del tipo', type(variable2))

print(variable3, 'es del tipo', type(variable3))
print(variable4, 'es del tipo', type(variable4))

print(variable5, 'es del tipo', type(variable5))
print(variable6, 'es del tipo', type(variable6))

